.env
__pycache__/
*.pyc
.venv/
permits/
*.db

# Licensing — the installed key is per-deployment; the private key must NEVER
# be committed. The public key (license_public_key.pem) IS committed.
license.key
*license_private*.pem
