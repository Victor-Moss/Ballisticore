# ── Secrets / local config ──────────────────────────────────────────────────
.env
*.env
!*.env.template
!env*.template
# Messaging provider credentials (Telegram bot token / Twilio auth token) live
# here once configured — treat like .env: never commit, never bundle.
BallistiCore_app/backend/config.json

# ── Python ──────────────────────────────────────────────────────────────────
.venv/
__pycache__/
*.pyc
.pytest_cache/

# ── Node / frontend build ───────────────────────────────────────────────────
node_modules/
dist/

# ── Installer staging / output ──────────────────────────────────────────────
installer/payload/
installer/dist/

# ── Runtime data (never source) ─────────────────────────────────────────────
pgdata/
permits/
logs/
backups/
*.log

# ── Editor / OS ─────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db

# ── Local dev helpers (never source) ────────────────────────────────────────
# Sensitive ops notes — keep out of the repo
How to generate a new license key.txt
# Local dev launcher scripts
/run_backend.bat
/run_frontend.bat
/start_dev.bat
/stop_dev.bat
